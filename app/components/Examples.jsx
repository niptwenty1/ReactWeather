var React = require('react');

var Examples = (props) =>{
    return (
      <div>
        <p>Welcome to examples</p>
        <h2>Examples Component!</h2>
      </div>

    );
  };

module.exports = Examples;
